/**
 * Contains app config that is static and not environment dependent like project flavor or build type.
 * For environment config use [BuildConfig] fields in `build.gradle`
 */
package mk.webfactory.template.config

